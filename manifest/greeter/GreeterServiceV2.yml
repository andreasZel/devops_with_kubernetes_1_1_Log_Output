apiVersion: v1
kind: Service
metadata:
  name: greeter-svc-2
  namespace: default
  labels:
    app: greeter
    version: v2
spec:
  type: ClusterIP
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
      name: http
  selector:
    app: greeter
    version: v2
